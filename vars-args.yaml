- name: variables from args
  hosts: localhost
  connection: local
  tasks:
  - name: print the course info
    ansible.builtin.debug:
     msg: "course_name: {{COURSE}},Course_duration: {{DURATION}},name of institute: {{Institute}}"